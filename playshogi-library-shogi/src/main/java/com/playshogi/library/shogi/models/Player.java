package com.playshogi.library.shogi.models;

public enum Player {
    BLACK, // Sente or Shitate (handicap taker), normally represented at the lower side of the board
    WHITE // Gote or Uwate (handicap giver), normally represented at the upper side of the board
}
